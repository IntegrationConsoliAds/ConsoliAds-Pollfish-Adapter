use_frameworks!

platform :ios, '9.0'

target 'ConsoliAds-Pollfish-Adapter_Example' do
  pod 'ConsoliAds-Pollfish-Adapter', :path => '../'

  target 'ConsoliAds-Pollfish-Adapter_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase'
  end
end
